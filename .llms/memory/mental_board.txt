Current goal: Better Age MVP reimplementation, Phase 3 complete.

Active state:
- Phase 1 skeleton edits done.
- Current CLI moved to `packages/cli-legacy`; package private; no `bage` bin.
- New `packages/core` skeleton exists.
- New `packages/cli` skeleton exists; no `bage` bin yet.
- `packages/varlock` unchanged.
- Append-only implementation log: `.llms/projects/4-cli-core-boundary-hardening/11-IMPLEMENTATION_LOG.md`.
- Phase 2 core artifact foundation started with TDD.
- Core has v1 artifact schemas/parsers/encoders/no-op migrations for home, private-key plaintext, payload plaintext, payload document, public identity.
- Public identity string round-trips as `better-age://identity/v1/<base64url-json>`.
- Core test passed.
- Root `pnpm check` passed.
- Phase 3 identity/key lifecycle implemented over fake ports.
- Core now supports setup, export, import/list/forget known identity, self summary, retired key summary, rotate, passphrase change.
- Phase 3 tests: 15 unit tests passing.

Next:
- Phase 4: core payload lifecycle over fake ports.

Blockers:
- None currently.
