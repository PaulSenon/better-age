Current goal: security release hardening + documentation alignment complete; await user QA.

Active state:
- Phase 1-7 complete.
- Review follow-up fixed key backup cleanup rollback risk.
- Review follow-up fixed private key ref path traversal risk.
- Review follow-up fixed missing key file failure mapping.
- Review follow-up fixed untrusted identity display-name terminal injection in CLI output/picker labels.
- Review follow-up fixed non-atomic encrypted payload writes with same-directory `<target>.tmp` then rename.
- Documentation pass aligned Markdown outside `.llms` with current MVP commands, core/CLI boundaries, security behavior, and QA checklist.
- Latest verification passed: `pnpm check`, `git diff --check`.

Next:
- User runs manual QA from `docs/manual-qa.md`.
- Fix any QA regressions found.

Blockers:
- Release readiness depends on user manual QA pass.
