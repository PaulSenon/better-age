Current goal: Better Age MVP reimplementation, Phase 7 complete.

Active state:
- Phase 1 workspace split done: legacy CLI is `packages/cli-legacy`, private, no `bage` bin.
- Phase 2 artifact/migration foundation done.
- Phase 3 core identity/key lifecycle done.
- Phase 4 core payload lifecycle done.
- Phase 5 real core adapters done.
- Phase 6 new CLI identity slice done.
- Phase 7 new CLI payload create/read/edit slice done.
- Append-only implementation log: `.llms/projects/4-cli-core-boundary-hardening/11-IMPLEMENTATION_LOG.md`.

Phase 7 notes:
- `runCli` now supports `create`, `inspect`, `view`, `load --protocol-version=1`, and `edit`.
- Payload context opening handles path preflight, headless passphrase failure, passphrase retry, and update notices.
- `load` keeps stdout raw env text only.
- `view` sends plaintext only to `terminal.openViewer`.
- `edit` uses `terminal.openEditor` and covers cancel/unchanged/invalid retry/changed save.
- `createNodeCli` passes real payload existence preflight.
- No `bage` bin yet.
- `@effect/cli` dependency exists, but command tree/parser integration still pending.

Latest verification:
- `pnpm test` passed.
- `pnpm check` passed.
- `git diff --check` passed.

Next:
- Phase 8: new CLI sharing and maintenance slice (`grant`, `revoke`, `update`).

Blockers:
- None currently.
