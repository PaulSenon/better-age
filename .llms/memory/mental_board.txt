Current goal: Better Age MVP reimplementation, Phase 6 complete.

Active state:
- Phase 1 workspace split done: legacy CLI is `packages/cli-legacy`, private, no `bage` bin.
- Phase 2 artifact/migration foundation done.
- Phase 3 core identity/key lifecycle done over fake ports.
- Phase 4 core payload lifecycle done over fake ports.
- Phase 5 real core adapters done: node home/payload repos + age identity/payload crypto.
- Phase 6 new CLI identity slice done.
- Append-only implementation log: `.llms/projects/4-cli-core-boundary-hardening/11-IMPLEMENTATION_LOG.md`.

Phase 6 notes:
- New CLI runner: `packages/cli/src/cli/runCli.ts`.
- Presenter boundary: `packages/cli/src/cli/presenter.ts`.
- Real adapter wiring helper: `packages/cli/src/cli/nodeCli.ts`.
- CLI covers setup, identity export/import/list/forget contracts.
- Core import now preserves existing alias when alias omitted.
- No `bage` bin yet.
- `@effect/cli` dependency exists, but command tree/parser integration still pending.

Latest verification:
- `pnpm test` passed.
- `pnpm check` passed.
- `git diff --check` passed.

Next:
- Phase 7: new CLI payload read/edit slice.

Blockers:
- None currently.
