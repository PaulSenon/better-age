Current goal: Better Age MVP reimplementation, Phase 8 complete.

Active state:
- Phase 1 workspace split done: legacy CLI is `packages/cli-legacy`, private, no `bage` bin.
- Phase 2 artifact/migration foundation done.
- Phase 3 core identity/key lifecycle done.
- Phase 4 core payload lifecycle done.
- Phase 5 real core adapters done.
- Phase 6 new CLI identity slice done.
- Phase 7 new CLI payload create/read/edit slice done.
- Phase 8 new CLI sharing/maintenance slice done.
- Append-only implementation log: `.llms/projects/4-cli-core-boundary-hardening/11-IMPLEMENTATION_LOG.md`.

Phase 8 notes:
- `runCli` now supports `grant`, `revoke`, and `update`.
- Exact grant resolves known refs, payload recipient refs, and identity strings via injected parser.
- Guided grant/revoke use `terminal.selectOne` picker options.
- Outdated write gate applies to exact/guided mutation flows; guided update-now resumes.
- Core payload recipient summaries now expose `publicKey` and `identityUpdatedAt`.
- No `bage` bin yet.
- `@effect/cli` dependency exists, but command tree/parser integration still pending.

Latest verification:
- `pnpm test` passed.
- `pnpm check` passed.
- `git diff --check` passed.

Next:
- Phase 9: CLI identity security slice (`identity rotate`, `identity passphrase`).

Blockers:
- None currently.
