Current goal: Better Age MVP reimplementation, Phase 1.

Active state:
- Phase 1 skeleton edits done.
- Current CLI moved to `packages/cli-legacy`; package private; no `bage` bin.
- New `packages/core` skeleton exists.
- New `packages/cli` skeleton exists; no `bage` bin yet.
- `packages/varlock` unchanged.
- Append-only implementation log: `.llms/projects/4-cli-core-boundary-hardening/11-IMPLEMENTATION_LOG.md`.

Next:
- Run final read-only hygiene for Phase 1.
- Then proceed to Phase 2: artifact + migration foundation.

Blockers:
- No build/test/check run; user forbids dev commands unless explicitly requested.
