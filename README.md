# better-age

CLI-only monorepo for the `better-age` toolchain.

Packages:
- `@better-age/cli`
- `@better-age/varlock`

Root commands:

```bash
pnpm build
pnpm lint
pnpm typecheck
pnpm test
pnpm check
```

CLI package publishes the `bage` executable.
